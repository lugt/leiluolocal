package matrice.alpha.Action;

import matrice.alpha.Complexity.Environment;

/**
 * Created by Frapo on 2017/1/21.
 */
public class Catastrophe implements IAction{
    /**
    * Everything comes in pair!
    * Stability suddenly changes to another form (from inoput to output)
    *（Creating new features While annilating many of the previous feature pairs()）
    *
    * From Respiration to
    *
    * */
    public Object apply(Object a, Environment env){
        return new int[1];
    }
}
